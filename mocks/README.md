# Mocks (Anthropic parity)

Contains **`mock-anthropic-service`** and **`parity/mock_parity_scenarios.json`** for CLI parity testing. Tracked on development branches; the **`deployed`** branch omits this tree (see xlotyl `docs/project_status/canonical-branches.md`).

Run `cargo` from `rust/`; workspace members include `../mocks/mock-anthropic-service`.
